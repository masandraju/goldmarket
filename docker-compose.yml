services:
  postgres:
    image: postgres:16-alpine
    container_name: goldmarket_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-goldmarket}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-goldmarket123}
      POSTGRES_DB: ${POSTGRES_DB:-goldmarket_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U goldmarket"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: goldmarket_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
